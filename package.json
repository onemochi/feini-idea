{
  "name": "feini-contract-engine",
  "productName": "菲尼合同引擎",
  "version": "1.0.6",
  "description": "Excel 智能合同生成 + Windows 系统级 OCR PDF 拆分",
  "main": "electron/main.js",
  "author": "feini",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win --x64",
    "build:portable": "electron-builder --win portable --x64",
    "release": "electron-builder --win nsis portable --x64 && node scripts/release.js"
  },
  "devDependencies": {
    "electron": "27.3.11",
    "electron-builder": "24.13.3"
  },
  "build": {
    "appId": "com.feini.contract.engine",
    "productName": "菲尼合同引擎",
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron/**/*",
      "index.html",
      "ceshi/**/*",
      "!**/node_modules/*/{CHANGELOG.md,README.md,README,readme.md,readme}",
      "!**/node_modules/*/{test,__tests__,tests,powered-test,example,examples}",
      "!**/*.map"
    ],
    "extraResources": [
      {
        "from": "electron/ocr/win-ocr.ps1",
        "to": "ocr/win-ocr.ps1"
      },
      {
        "from": "ceshi",
        "to": "ceshi"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "菲尼合同引擎"
    },
    "portable": {
      "artifactName": "菲尼合同引擎-便携版-${version}.exe"
    },
    "publish": [
      {
        "provider": "generic",
        "url": "https://feini-gugugaga.netlify.app/netlify-site/updates/",
        "channel": "latest"
      }
    ]
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  }
}
